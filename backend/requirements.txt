fastapi>=0.110.0
uvicorn[standard]>=0.29.0
asyncpg>=0.29.0
pydantic>=2.6.0
pydantic-settings>=2.2.0
httpx>=0.27.0
shapely>=2.0.0
geojson>=3.1.0
numpy>=1.26.0
scipy>=1.12.0
python-dotenv>=1.0.0
osmium>=3.7.0

# Phase 2: background scheduling
apscheduler>=3.10.0

# Phase 5: LSTM AQI forecasting
# CPU-only torch is sufficient — model is tiny, inference < 5ms
# Install CPU-only to keep image size reasonable:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.2.0
