fastapi>=0.110.0
uvicorn[standard]>=0.29.0
asyncpg>=0.29.0
pydantic>=2.6.0
pydantic-settings>=2.2.0
httpx>=0.27.0
shapely>=2.0.0
geojson>=3.1.0
numpy>=1.26.0
scipy>=1.12.0
python-dotenv>=1.0.0

# OSM parsing — replaces pyrosm which had reliability issues on large PBFs
# Requires the osmium C++ library: sudo apt install libosmium-dev
osmium>=3.7.0
